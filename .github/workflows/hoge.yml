name: GitHub Actions Demo

on:
  workflow_dispatch:

jobs:
  check_pr_title:
    runs-on: ubuntu-latest
    steps:
      - name: リポジトリを取得する
        uses: actions/checkout@v4

      - name: モジュールのインストール
        run: npm install

      - name: danger JSの実行
        run: node ./dangerfile.js
